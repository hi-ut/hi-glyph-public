import React from 'react'

function adminGlyphsLayout(props: { children: React.ReactNode }) {
  return (
    <div>
      <div>{props.children}</div>
      
    </div>
  )
}

export default adminGlyphsLayout